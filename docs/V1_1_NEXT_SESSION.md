# Over the Luna v1.1 — next-session bootstrap

Updated: **2026-08-20 (KST)**

This is a deliberately short bootstrap for a fresh ChatGPT/Copilot session. **Do not reconstruct the research from commit history.** Read the durable handoff and continue from the runtime gate.

## Read in this order

1. `docs/V1_1_HANDOFF.md` on `research/v1.1-runtime-baseline` — single source of truth for decisions, evidence, branches, run IDs, rejected ideas, and exact next gates.
2. `experiments/VSCODE_GATE_A_AUTOMATED_RESULTS_2026-08-20.md` — automated Main-wiring evidence.
3. PR **#15** / branch `rc/v1.1-vscode-gate-ambient` / `docs/V1_1_VSCODE_GATE.md` — **leading real VS Code Gate A candidate and exact manual checklist**.
4. PR **#16** / branch `rc/v1.1-vscode-gate-schema` — fallback only if ambient wiring fails a product-critical subagent/council-selection gate.

## Current product state

Do not reopen automated-core architecture research unless real VS Code falsifies it.

- Automated Luna core: **PRE-PRODUCTION RC2, 8/8 final tiny/local/broad/risk matrix PASS**.
- Investigation: `SIMPLE / STANDARD / DEEP`.
- Assurance: separate `NONE / REVIEW / RISK`.
- Main: sole mutation owner.
- Broad unknown semantic discovery: Luna Architect owns the evidence pass and returns a sealed work set.
- Normal REVIEW: exactly one named artifact-first Luna Reviewer for the trajectory.
- Reviewer: bounded dependency closure + one consequential invariant challenge.
- Premium UX: **one visible human-selected `Premium Review`, Sonnet 5 candidate, `send:false`**. Do not resurrect the routine Sonnet/Opus two-button menu without new evidence.
- Released `main`: still **v1.0.0**. Do not bump to `1.1.0` yet.

## The only decisive blocker now: real VS Code Gate A

Start with **PR #15 ambient candidate**. Main intentionally omits both `tools` and `agents`; VS Code owns the developer-selected tool environment while Main instructions restrict delegation to the exact seven Over the Luna leaves.

In an authenticated VS Code Agent Plugin runtime, execute the checklist in `docs/V1_1_VSCODE_GATE.md` and record:

1. plugin/custom-agent discovery has no diagnostics error;
2. `agent/runSubagent` is actually available to ambient Main;
3. unknown broad semantic discovery selects exact `Luna Architect`, not another installed custom agent;
4. a **known policy-permitted developer-selected MCP/extension tool** remains usable zero-config;
5. leaf least privilege still holds;
6. sufficient Architect handback causes no Main broad repository replay;
7. tiny/local/broad/risk route + assurance shapes survive real runtime;
8. exactly one `Premium Review` handoff renders and switches to the exact custom agent;
9. Premium remains human initiated (`send:false`) and model unavailability/substitution is not silently misrepresented;
10. capture Agent Debug/OTel metadata for at least SIMPLE+REVIEW and STANDARD+REVIEW.

### Decision rule

- If ambient passes, freeze it as the v1.1 Main wiring.
- If ambient materially fails subagent capability or exact Council selection, test **PR #16 schema-strict fallback**.
- If ambient loses a selected MCP/extension tool **and that tool is known to be permitted by the active Copilot policy**, treat it as real evidence against ambient.
- Do not confuse outer organization/enterprise MCP policy blocks with an Over the Luna frontmatter failure.
- Do not use `tools: ['*', 'agent']`; that compromise was already rejected by runtime evidence.

## After Gate A passes

Proceed directly to Gate C productization; do not invent another architecture phase:

1. freeze the selected Main wiring;
2. finalize real `agents/` release layout;
3. update README / README.ko / DESIGN / SMOKE_TEST / CONTRIBUTING / MCP guidance;
4. run packaged-plugin clean-install smoke tests;
5. decide which research artifacts remain;
6. bump `plugin.json` and changelog to **1.1.0**;
7. prepare release integration into `main`.

## Hygiene before doing anything expensive

- Check current heads of PR #13, #15, and #16 first; branches may have advanced.
- Confirm `.github/workflows` contains **no `*_once.yml` residue** before any new paid probe.
- Persistent paid research workflows must remain `workflow_dispatch` only.
- Do not launch another paid CLI MCP inheritance experiment while the relevant server is blocked by outer Copilot policy; the next meaningful evidence is authenticated real VS Code with a policy-permitted selected tool.

If a new session has only one sentence of context, use this:

> Continue Over the Luna v1.1 from `docs/V1_1_NEXT_SESSION.md`, then read `docs/V1_1_HANDOFF.md`; do not redo the automated-core research, start with real VS Code Gate A on draft PR #15 and use #16 only as fallback.
