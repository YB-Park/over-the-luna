name: Validate plugin

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.12'

      - name: Install validator dependency
        run: python -m pip install 'PyYAML==6.0.2'

      - name: Compile Python controller code
        run: |
          python -m compileall -q scripts
          python -m compileall -q experiments/premium_v2_1_plugin/scripts

      - name: Validate stable plugin and agents
        run: python scripts/validate_plugin.py

      - name: Validate Premium v2.1 experimental plugin
        run: python scripts/validate_premium_v2_1_plugin.py

      - name: Run Premium v2.1 zero-AI controller tests
        run: python -m unittest discover -s tests -p 'test_*.py' -v
